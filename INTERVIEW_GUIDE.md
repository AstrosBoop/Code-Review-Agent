# 🎯 面试通关秘籍 (Interview Guide)

这份指南专为您在面试中展示 **Agentic Code Review System** 项目量身定制。它提取了项目中的核心技术难点和架构亮点，并以 **STAR 法则 (Situation, Task, Action, Result)** 呈现，帮助您在面试中侃侃而谈。

---

## 🚀 1. 项目电梯演讲 (Elevator Pitch)
**当面试官问：“请介绍一下你简历上的这个 AI 代码审查系统项目。”**

> “这是一个基于 **FastAPI 和 LangGraph** 构建的生产级多智能体（Multi-Agent）代码审查系统。
> 它的核心亮点是**将传统的单点 LLM 调用升级为了流水线化的智能体协同**：我设计了安全、质量、性能三个独立 Agent 并行分析代码，最后由 Synthesizer 节点聚合成一份结构化报告。
> 
> 为了解决大模型推理耗时带来的 HTTP 连接超时问题，我引入了 **FastAPI Background Tasks + 客户端长轮询** 的异步架构，配合 SQLite 实现了全链路的请求追踪。此外，项目通过 Docker 实现了容器化部署，支持多模型提供商无缝切换。这不仅是一个 AI Demo，而是一个具备高可用性和良好工程实践的后端系统。”

---

## ⭐ 2. 核心亮点与 STAR 故事

### 故事一：如何解决 LLM 响应慢导致的请求超时问题？(异步化架构)
**Situation (情境)**：大模型（尤其是并行调用多个 Agent 时）生成代码审查报告通常需要 10-30 秒，如果让前端通过同步 HTTP 请求等待结果，极易导致网关超时（504 Gateway Timeout）和极差的用户体验。
**Task (任务)**：需要重构 API 交互逻辑，实现非阻塞的代码提交和结果获取。
**Action (行动)**：
1. 废弃了同步等待模式，设计了 `POST /review` 和 `GET /review/{id}/status` 的分离式 API。
2. 引入 **FastAPI Background Tasks**。当用户提交请求时，后端立即在 SQLite 数据库中创建一条 `status='pending'` 的记录，并秒回 `review_id` 给前端，随后将 LangGraph 的运行逻辑推入后台任务池执行。
3. 前端拿到 `review_id` 后，开启 `setInterval` 轮询，直到状态变更为 `completed` 或 `failed`，再拉取最终报告。
**Result (结果)**：接口响应时间从 20 秒骤降至 50 毫秒级别，系统吞吐量大幅提升，且彻底消除了连接超时隐患，用户可以通过 UI 实时看到任务状态流转。

### 故事二：为什么要用 LangGraph，而不是直接写个 Prompt 让 ChatGPT 审？(架构演进)
**Situation (情境)**：最初的原型是拼接一个巨大的 Prompt 让 LLM 一次性审查代码，但发现模型经常“顾此失彼”，比如指出了代码规范问题，却漏掉了严重的 SQL 注入漏洞。
**Task (任务)**：需要提高 AI 审查的精准度、覆盖率，同时保证系统具有良好的可扩展性。
**Action (行动)**：
1. 引入 **LangGraph**，将审查任务进行**分治 (Divide and Conquer)**。
2. 定义了包含 `code`, `security_issues`, `quality_issues`, `performance_issues` 的全局状态（State）。
3. 拆分出 3 个职责单一的专业 Agent（安全、质量、性能），它们利用 `RunnableParallel` 实现并行计算，互不干扰地向 State 中写入自己的发现。
4. 最后加入一个 Synthesizer Agent，负责将三个维度的内容融合成一份专业的 Markdown 报告，并打分和评级。
**Result (结果)**：审查覆盖率显著提升，漏报率大幅下降。更重要的是，系统具有了极强的扩展性（未来如果要加“合规审查 Agent”，只需在图中加一个节点即可，无需修改其他逻辑）。

### 故事三：如何保证系统在不同环境下的稳定性与数据安全？(工程化与容器化)
**Situation (情境)**：项目在本地运行良好，但在换机测试或给别人演示时，经常遇到依赖冲突、环境变量缺失、以及数据重新生成丢失的问题。
**Task (任务)**：将应用进行标准化打包，确保一键部署，并实现数据的持久化。
**Action (行动)**：
1. 编写了精简的 `Dockerfile`，采用 `python:3.12-slim` 基础镜像，严格分离依赖安装与代码拷贝，优化了构建缓存。
2. 设计了健壮的数据库路径解析（`os.getenv("DB_PATH")`），本地开发使用相对路径，Docker 环境使用挂载路径。
3. 编写 `docker-compose.yml`，将宿主机的 `./data` 目录挂载到容器内，保证 SQLite 数据库的持久化。添加了完善的 `.dockerignore` 避免密钥泄露。
**Result (结果)**：实现了一键部署 `docker-compose up -d`，哪怕容器被销毁重启，历史审查记录和 Request ID 追踪数据也完好无损，达到了小型生产环境的标准。

---

## ❓ 3. 面试官高频追问及参考回答

**Q: 为什么选用 SQLite 而不是 MySQL / PostgreSQL？**
**A**: “对于这个项目的 MVP（最小可行性产品）阶段，SQLite 是最完美的选择。它不需要额外部署数据库服务，极大地降低了部署门槛。由于 Python 的 `sqlite3` 是内建的，这让整个项目可以无外部依赖地跑起来。虽然 SQLite 在高并发写操作下存在锁表问题，但当前系统的特点是**低频写入（提交任务、更新状态）、单点执行**，完全能 hold 住。在架构设计上，我将数据库操作封装在了 `DBManager` 类中，如果未来业务量爆发，只需重写这个类接入 SQLAlchemy 和 Postgres，业务逻辑完全无需改动。”

**Q: 你的前端轮询是多久一次？为什么不考虑 WebSocket 或 Server-Sent Events (SSE)？**
**A**: “前端目前设定的是 1.5 秒轮询一次。之所以选择短轮询，是因为当前场景具有**确定性的有限时间延迟**（一般 10-20 秒出结果），且并发量未达到需要严格节约 HTTP 握手开销的程度，短轮询实现成本最低、健壮性最强（天然防断连）。
当然，我也考虑过 **WebSocket** 和 **SSE**。如果后续系统升级到能‘实时流式输出每个 Agent 的思考过程’（Streaming），我肯定会重构为 SSE，因为 SSE 对单向的服务端推送（Server-to-Client）支持得非常好，且能复用 HTTP/2 特性，比 WebSocket 更轻量。”

**Q: 代码里的 Mock Fallback 机制是怎么考虑的？**
**A**: “这是一种**防御性编程**的体现。在实际开发调试，或者给面试官展示、甚至网络环境不佳时，第三方 API 极易超时或报错。如果因此导致整个系统宕机，体验会非常差。我设计了异常捕获并降级到 Mock 数据，保证主流程能够无缝流转，前端不仅能照常渲染，还会在 Header 给出红色的 `Mock Fallback` 警告，兼顾了系统可用性和工程透明度。”

---
*祝您面试顺利，用这个项目惊艳面试官！* 🚀
